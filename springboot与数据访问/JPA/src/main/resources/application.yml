server:
  port: 9090
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpa?serverTimezone=UTC
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
#      更新或更改数据表结构
      ddl-auto: update
#    控制台显示SQL
    show-sql: true